pipeline {
    agent any

    stages {
        stage("Build") {
            steps {
                echo "Some code compilation here..."
            }
        }
        
        stage("Test") {
            steps {
                echo "Some code compilation here..."
            }
        }    

        stage("Release") {
            steps {
                echo "Some tests execution here..."
                echo "1"
            }
        }
    }
}
