upstream backend {
      server localhost:7777;
}

server {
     listen 80;
     listen [::]:80;

     server_name test.sukereddy.xyz;

     location /{
          proxy_read_timeout 600s;
          proxy_pass http://backend;
   }
}
