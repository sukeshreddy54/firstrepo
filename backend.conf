upstream backend {
      server localhost:7777;
}

server {
     listen 80;
     listen [::]:80;

     server_name 3.93.65.114;

     location /{
          proxy_read_timeout 600s;
          proxy_pass http://backend;
   }
}
